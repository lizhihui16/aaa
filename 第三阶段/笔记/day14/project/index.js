/*
1. 轮播图 :
	1. 获取所有图片标签
	2. 每隔一秒切换(变更下标,取图片元素,控制显示与隐藏)
		var imgs;
		var imgIndex = 0;
		setInterval()
		function (){
			imgIndex ++;
			imgIndex对应的元素是显示的,其他的隐藏
			下标越界判断
		}
2. 轮播图下标
	显示在图片上方
*/